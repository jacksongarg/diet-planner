import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - Diet Planner',
  description: 'Create a new Diet Planner account',
};

export default function SignupPage() {
  return <SignupForm />;
}
